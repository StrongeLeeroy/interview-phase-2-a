import { Component, Input } from "@angular/core";

@Component({
    selector: 'app-post-preview',
    template: ``,
    styles: [`
        /**
         * Suggested styles
         */
        .post-title {
            margin-bottom: 5px;
        }
        .post-body {
            margin-top: 0;
        }
    `]
})
export class PostPreviewComponent {}